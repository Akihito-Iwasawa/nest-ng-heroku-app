{
  "name": "root",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "packages": [
    "client/**",
    "server/**"
  ]
}

{
  "name": "root",
  "private": true,
  "packages": [
    "client/**",
    "server/**"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}

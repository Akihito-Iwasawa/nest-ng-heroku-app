{
  "name": "root",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "packages": [
    "client/*",
    "server/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream"
  },
  "dependencies": {
    "@angular/cli": "~13.2.4",
    "@angular/compiler-cli": "~13.2.0"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
